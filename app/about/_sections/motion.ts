/**
 * Shared motion constants for About-page sections.
 *
 * `slowBurn` is the page-wide easing curve used by every motion moment
 * on /about. Lives in one place so timing stays in lockstep across
 * Foundation, Long View, Manifesto, Timeline, etc.
 */
export { easeSlowBurn as slowBurn } from "@/lib/motion";
